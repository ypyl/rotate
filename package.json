{
  "name": "elm-weektodo",
  "private": true,
  "version": "1.0.16",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "neu": "npx @neutralinojs/neu run --frontend-lib-dev",
    "neu-release": "npx @neutralinojs/neu build --release",
    "neu-build": "npx @neutralinojs/neu build",
    "neu-run": "npx @neutralinojs/neu run",
    "sync-version": "node ./sync-version.js"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^9.5.0",
    "elm": "^0.19.1-5",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vite-plugin-elm": "^2.8.0-beta.3"
  },
  "dependencies": {
    "@neutralinojs/lib": "^3.8.2"
  }
}
